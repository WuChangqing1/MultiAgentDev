[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# The file sandbox used during development cannot create pytest's cache
# directory, so it is disabled explicitly rather than warned about.
addopts = -q --strict-markers -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
