# Keep npm's cache inside the project so installs work under a file sandbox
# that only grants write access to the workspace, and so CI is reproducible.
cache=./.npm-cache
fund=false
audit=false
loglevel=error
